<div class="row">
	<div ui-sref="galleryslide" ng-repeat="img in gallery" class="col" ng-if="$index<3" >
		<img  src="{{img.url}}" style="width:100%;" />
	</div>
	
</div>
<div class="row">
	<div ui-sref="galleryslide" ng-repeat="img in gallery" class="col" ng-if="$index>=3">
		<img src="{{img.url}}" style="width:100%;" />
	</div>
	
</div>
